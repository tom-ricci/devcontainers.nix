# generated by generate-github-actions
# Do not edit.

name: build-image-frida-windows

concurrency:
  group: "devcontainers.nix-build-image-frida-windows"
  cancel-in-progress: true

on:
  workflow_dispatch:
  push:
    branches: ["master"]

# # buggy https://github.com/orgs/community/discussions/41518
# concurrency:
#   group: "tom-ricci-devcontainers-nix"
#   cancel-in-progress: false

jobs:


  build-frida-windows-amd64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-latest
      package: frida-windows
      free-disk-space: true




  manifest:
    needs:

      - build-frida-windows-amd64


    uses: ./.github/workflows/manifest.yml
    with:
      package: frida-windows
      free-disk-space: true
