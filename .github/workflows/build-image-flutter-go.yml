# generated by generate-github-actions
# Do not edit.

name: build-image-flutter-go

concurrency:
  group: "devcontainers.nix-build-image-flutter-go"
  cancel-in-progress: true

on:
  workflow_dispatch:
  push:
    branches: ["master"]

# # buggy https://github.com/orgs/community/discussions/41518
# concurrency:
#   group: "tom-ricci-devcontainers-nix"
#   cancel-in-progress: false

jobs:


  build-flutter-go-amd64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-latest
      package: flutter-go
      free-disk-space: true




  manifest:
    needs:

      - build-flutter-go-amd64


    uses: ./.github/workflows/manifest.yml
    with:
      package: flutter-go
      free-disk-space: true
