# generated by generate-github-actions
# Do not edit.

name: build-image-dotnet-8

concurrency:
  group: "devcontainers.nix-build-image-dotnet-8"
  cancel-in-progress: true

on:
  workflow_dispatch:
  push:
    branches: ["master"]

# # buggy https://github.com/orgs/community/discussions/41518
# concurrency:
#   group: "tom-ricci-devcontainers-nix"
#   cancel-in-progress: false

jobs:


  build-dotnet-8-amd64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-latest
      package: dotnet-8
      free-disk-space: true




  manifest:
    needs:

      - build-dotnet-8-amd64


    uses: ./.github/workflows/manifest.yml
    with:
      package: dotnet-8
      free-disk-space: true
