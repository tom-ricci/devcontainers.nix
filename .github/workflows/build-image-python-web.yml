# generated by generate-github-actions
# Do not edit.

name: build-image-python-web

concurrency:
  group: "devcontainers.nix-build-image-python-web"
  cancel-in-progress: true

on:
  workflow_dispatch:
  push:
    branches: ["master"]

# # buggy https://github.com/orgs/community/discussions/41518
# concurrency:
#   group: "tom-ricci-devcontainers-nix"
#   cancel-in-progress: false

jobs:


  build-python-web-amd64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-latest
      package: python-web
      free-disk-space: true




  manifest:
    needs:

      - build-python-web-amd64


    uses: ./.github/workflows/manifest.yml
    with:
      package: python-web
      free-disk-space: true
