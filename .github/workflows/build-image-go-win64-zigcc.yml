# generated by generate-github-actions
# Do not edit.

name: build-image-go-win64-zigcc

concurrency:
  group: "devcontainers.nix-build-image-go-win64-zigcc"
  cancel-in-progress: true

on:
  workflow_dispatch:
  push:
    branches: ["master"]

# # buggy https://github.com/orgs/community/discussions/41518
# concurrency:
#   group: "tom-ricci-devcontainers-nix"
#   cancel-in-progress: false

jobs:


  build-go-win64-zigcc-amd64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-latest
      package: go-win64-zigcc
      free-disk-space: true




  manifest:
    needs:

      - build-go-win64-zigcc-amd64


    uses: ./.github/workflows/manifest.yml
    with:
      package: go-win64-zigcc
      free-disk-space: true
